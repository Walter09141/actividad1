
// Ejercicio 3: Intercambiar el valor de 2 variable
const Leer = requiere('prompt-sync')
constLeer = console
class condicion {
principal(){
	
let a,b,aux  
	
a. Leer ("Digite el valor de a: ");
	a;
b. Leer ("Digite el valor de b: ");
	b;
	
	aux <- a;
	a <- b;
	b <- aux;
	
	Escribir.log ("El nuevo valor de a es: "),a;
	Escribir.log ("El nuevo valor de b es: "),b;
  }	
}
let a=new principal()
a.principal()
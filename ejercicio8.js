
const Leer = requiere('prompt-sync')
constLeer = console
class condicion {
principal() {
	let a,b,c,resultado 
	
	Escribir.log ("Digite el valor de A: ");
	a;
 	Escribir.log ("Digite el valor de B: ");
	b;
    Escribir.log ("Digite el valor de c: ");
	c;
	
	resultado <- (-b + rc(b^2 - 4*a*c))/(2*a);
	
	Escribir.log ("El resultado es: ",resultado);
  }
}

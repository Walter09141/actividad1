
// Ejercicio 1: Calcular la suma de los "N" primeros n�meros.
const Leer = requiere('prompt-sync')
constLeer = console
class condicion {
 ejercicio23() {
	let n,suma,i 
	Escirbir.log ("Digite la cantidad de numeros a sumarse: ",);
	n = Number(prompt());
	suma = 0;
	for (i=1;i<=n;i++) {
		suma = suma+i;
	}
	Escirbir.log ("La suma es: ",suma,);
}
}

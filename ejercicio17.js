
// Ejercicio 5: Leer tres numeros diferentes e imprimir
// el n�mero de los tres.

const Leer = requiere('prompt-sync')
constLeer = console
class condicion {
ejercicio5() {
	let num1,num2,num3
	Leer ("Digite tres numeros diferentes: ",);
	num1 = Number(prompt());
	num2 = Number(prompt());
	num3 = Number(prompt());
	if (num1>num2 && num1>num3) {
		Escribir.log ("El mayor es; ",num1,)
	
			if (num2>num1 && num2>num3) {
			Escribir.log ("El mayor es; ",num2,);
		} else {
			Escirbir.log ("El mayor es: ",num3,);
		}
	}
}
}
